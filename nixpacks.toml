[phases.setup]
nixPkgs = ["php", "php82Packages.composer", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
